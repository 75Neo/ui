/**
 * The page gutter, shared by every band so the header rule, the hero and the body all
 * line up on the same two edges.
 */
export const shell = "mx-auto w-full max-w-page px-5 sm:px-8 lg:px-12";
