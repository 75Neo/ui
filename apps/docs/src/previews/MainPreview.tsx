import { Main } from "@75neo/react/main";

export function MainPreview() {
  return (
    <Main className="grid min-h-32 place-items-center rounded-lg bg-muted text-sm text-toned">
      The content region, shown short here.
    </Main>
  );
}
