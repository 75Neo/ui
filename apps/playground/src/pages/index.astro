---
import Playground from "../layouts/Playground.astro";
import { previews } from "../routes";
---

<Playground title="Playground">
  <p class="text-muted mb-6 max-w-prose text-sm">
    Every component rendered from package source, React and Vue side by side. Edits to
    <code class="text-toned">packages/*/src</code> hot-reload here with no
    build step.
  </p>

  <ul class="flex flex-col gap-2 text-sm">
    {
      previews.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class="text-highlighted hover:text-muted underline underline-offset-4"
          >
            {label}
          </a>
        </li>
      ))
    }
  </ul>
</Playground>
