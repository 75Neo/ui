import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A password field with a visibility toggle. The trigger is a real button, so it is reachable by keyboard and announced.",
  },
] satisfies DemoExample[];
