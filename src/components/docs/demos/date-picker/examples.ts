import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A text input paired with a calendar. The grid is rendered from the machine's own week data, so locale and first day of week come for free.",
  },
] satisfies DemoExample[];
