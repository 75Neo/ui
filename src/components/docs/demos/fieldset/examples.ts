import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A native fieldset that groups related fields under one legend. Disabling the fieldset disables everything inside it.",
    client: "none",
  },
] satisfies DemoExample[];
