import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "One segment per part of the date, each typed and arrowed independently. The segments come from the locale, so the order follows it.",
  },
] satisfies DemoExample[];
