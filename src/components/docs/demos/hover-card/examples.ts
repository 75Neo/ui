import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A preview that opens on hover after a delay. It is supplementary by design, so nothing inside should be the only way to reach something.",
  },
] satisfies DemoExample[];
