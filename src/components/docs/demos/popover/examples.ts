import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A panel anchored to its trigger, with an arrow that points back at it. Unlike a dialog it does not trap focus or block the page.",
  },
] satisfies DemoExample[];
