import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A code rendered as SVG, so it stays sharp at any size, with a trigger that saves it as a PNG.",
  },
] satisfies DemoExample[];
