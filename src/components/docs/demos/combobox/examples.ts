import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A text field that filters a list. The filtering is yours, which is what lets the items come from a server just as easily as from an array.",
  },
] satisfies DemoExample[];
