import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A listbox in a popover with a hidden native select behind it, so the value submits with a plain form.",
  },
] satisfies DemoExample[];
