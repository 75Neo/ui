import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "One trigger and one panel. The panel animates on its measured height, so it works without a fixed size.",
  },
] satisfies DemoExample[];
