import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A countdown split into its own part per unit, with start, pause and reset actions. The formatted values come from the machine.",
  },
] satisfies DemoExample[];
