import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "A draggable selection over an image, locked to an aspect ratio. The circular crop shape is a mask, so the exported region is still a square.",
  },
] satisfies DemoExample[];
