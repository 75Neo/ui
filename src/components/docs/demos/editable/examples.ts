import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "Text that turns into an input in place. Enter commits, escape reverts, and the triggers only render in the state where they apply.",
  },
] satisfies DemoExample[];
