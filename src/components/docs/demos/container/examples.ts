import type { DemoExample } from "@/components/docs/demos/types";

export default [
  {
    id: "overview",
    title: "Overview",
    description:
      "Centres its children and holds them to the width in --ui-container, with padding that steps up at the small and large breakpoints.",
    client: "none",
  },
] satisfies DemoExample[];
